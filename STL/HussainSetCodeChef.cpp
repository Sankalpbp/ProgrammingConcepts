# include <bits/stdc++.h>
using namespace std;

typedef long long L;

int main(void) {

    int n;
    int queries;
    cin >> n >> queries;

    L arr[n];
    for(int i=0; i<n; i++) {
        cin >> arr[i];
    }

    queue<L> q;

    int countOfQuery = 0;

    while(queries--) {
        int currentQuery;

        cin >> currentQuery;

        for(; )
    }

    return 0;
}
